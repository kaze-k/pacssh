[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
panic = "abort"

[package]
name = "ssh-pacman"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["hostname", "user", "term"] }
owo-colors = "4.4.0"
unicode-width = "0.2.2"
